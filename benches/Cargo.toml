[package]
name = "benches"
version = "0.1.0"
edition = "2024"

[dependencies]
remir = { path = "../remir", default-features = false }
remir_llvm = { path = "../remir_llvm" }

[[bench]]
name = "remir"
harness = false

[[bench]]
name = "llvm"
harness = false

[dev-dependencies]
criterion = "0.8.2"
